pipeline {
    agent any 
    
    tools {
        maven 'MAVEN_HOME'
    }
    stages {
        
        stage("Build Docker Image") {
            steps {
                script {
                    echo ${BUILD_NUMBER}
                    echo $BUILD_NU
                    echo BUILD_NU
                }
               
            }
        }
        
    }
}