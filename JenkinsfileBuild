pipeline {
    agent any 
    
    tools {
        maven 'MAVEN_HOME'
    }
    stages {
        
        stage("Build Docker Image") {
            steps {
                sh 'echo ${BUILD_NUMBER}'
                script {
                    echo 'BUILD_NU'
                }
               
            }
        }
        
    }
}